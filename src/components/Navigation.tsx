"use client";

export default function Navigation() {
  return (
    <header className="fixed top-0 left-0 right-0 z-50 flex items-center justify-end px-6 py-6 sm:px-16 mix-blend-difference">
      <a
        href="mailto:ccil.mp3@gmail.com"
        className="text-[8px] uppercase tracking-[0.4em] text-white/30 hover:text-white/60 transition-colors duration-1000"
      >
        Contact
      </a>
    </header>
  );
}
